# This static GitHub Pages build does not require runtime environment variables.
#
# Sensitive apartment data is encrypted before deployment with:
# PWA_ACCESS_PASSPHRASE="your-private-key" PRIVATE_SOURCE_DIR="../apartment-private-data" npm run encrypt-data
#
# Never commit the private key or the unencrypted private-data directory.
