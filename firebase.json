{
  "firestore": [
    {
      "database": "(default)",
      "rules": "firestore.default.rules"
    },
    {
      "database": "airbnb",
      "rules": "firestore.rules"
    }
  ],
  "storage": {
    "rules": "storage.rules"
  }
}

